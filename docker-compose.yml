services:
  forbidden-sector:
    build: .
    ports:
      - "9082:9082"
    volumes:
      - .:/app
    environment:
      - PYTHONPATH=/app
    command: uvicorn server:app --host 0.0.0.0 --port 9082 --reload
    restart: unless-stopped